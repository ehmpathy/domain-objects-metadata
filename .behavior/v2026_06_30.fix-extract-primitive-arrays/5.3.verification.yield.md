# verification — buttonup

metadata-only library (typescript-compiler-api AST introspection). the "contracts" are the
exported guards + the metadata that `introspect` / extract / hydrate return; the `.snap` files
are the contract-output demonstration (no cli / api / app screens exist).

## behavior coverage (wish + vision)

| behavior (from wish/vision) | test file | snapshot? | status |
|-----------------------------|-----------|-----------|--------|
| extract `string[]` / `number[]` / `boolean[]` / `Date[]` with nullable+optional preserved | `extract/extractPropertiesFromInterfaceDeclaration.test.ts` (PrimitiveArrays) | ✓ +77 | ✓ |
| `isPrimitiveArrayProperty` → true for primitive arrays, false for reference/enum | `guard/isPrimitiveArrayProperty.test.ts` | n/a | ✓ |
| `isReferenceArrayProperty` (renamed+narrowed) → true only for reference arrays | `guard/isReferenceArrayProperty.test.ts` | n/a | ✓ |
| `isEnumArrayProperty` → true only for enum arrays | `guard/isEnumArrayProperty.test.ts` | n/a | ✓ |
| shared `getArrayElementProperty` unwrap | `guard/getArrayElementProperty.test.ts` | n/a | ✓ |
| extract→hydrate→classify seam proven end-to-end (primitive + reference) | `hydrate/getHydratedDomainObjectMetadatasFromFiles.test.ts` (PrimitiveArrays) | ✓ +91 | ✓ |
| extract→hydrate→classify seam proven for enum arrays | `hydrate/getHydratedDomainObjectMetadatasFromFiles.test.ts` (AgentRole) | ✓ (unchanged) | ✓ |
| prior domain-object-array behavior unchanged (relations) | `hydrate/...test.ts` (Order.items) | ✓ (unchanged) | ✓ |
| old `isDomainObjectArrayProperty` fully removed | grep: zero refs in `src/` | n/a | ✓ |

## zero skips verified

- [x] no `.skip()` / `.only()` / `xit` / `xdescribe` — grep across `src/` returned no matches
- [x] no silent credential bypasses — library has no credentials/i-o boundaries (pure AST)
- [x] no prior failures carried forward — full thorough suite green

## snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| `extract/__snapshots__/extractPropertiesFromInterfaceDeclaration.test.ts.snap` | added (+77) | yes | new `PrimitiveArrays` asset — locks the extracted shape of `string[]`/`number[]`/`boolean[]`/`Date[]` + nullable/optional variants |
| `hydrate/__snapshots__/getHydratedDomainObjectMetadatasFromFiles.test.ts.snap` | added (+91) | yes | new `PrimitiveArrays` hydration case — locks the hydrated shape proven by the seam test |

- `git diff --stat main -- '*.snap'` → 168 insertions, 0 deletions. no extant snapshot was
  modified, so no regression to prior behavior. the AgentRole enum-array snapshot is unchanged
  (the seam test only added guard-classification assertions, not new metadata).

## tests executed — with proof

| suite | command | result | proof |
|-------|---------|--------|-------|
| types | `rhx git.repo.test --what types` | ✓ | exit 0, passed 5s |
| lint | `rhx git.repo.test --what lint` | ✓ | exit 0, biome + cycles + depcheck clean |
| format | `rhx git.repo.test --what format` | ✓ | exit 0, passed |
| unit (thorough) | `rhx git.repo.test --what unit --mode apply --thorough` | ✓ | exit 0, 17 suites, 60 tests passed, 0 failed, 0 skipped |
| integration | (no `.integration.test.ts` files exist) | n/a | pure library; `--passWithNoTests` |
| acceptance | (no `.acceptance.test.ts` files exist) | n/a | pure library; `--passWithNoTests` |

- [x] every test command was run and its output observed
- [x] exact exit codes verified (all 0)
- [x] no tests skipped, mocked, or faked

## contract output snapshot exhaustiveness

the library exposes no cli/api/app contract; the guard + introspect outputs are the contract.

| kind | contract | positive | negative | edge |
|------|----------|----------|----------|------|
| guard | `isPrimitiveArrayProperty` | ✓ string/number/boolean/date arrays | ✓ reference, enum, non-array | ✓ nullable/optional via seam |
| guard | `isReferenceArrayProperty` | ✓ reference array | ✓ string/number/enum, non-array | ✓ nested + by-uuid both REFERENCE |
| guard | `isEnumArrayProperty` | ✓ enum array | ✓ string, reference, non-array | ✓ via real hydrated AgentRole |
| introspect/extract shape | `PrimitiveArrays` / `AgentRole` metadata | ✓ snapshots | — | ✓ nullable/optional variants |

## blockers

- none.
