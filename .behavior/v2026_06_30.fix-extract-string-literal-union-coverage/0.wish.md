wish =

# wish: regression coverage for string-literal unions

## .what

lock in regression test coverage for string-literal unions (`'active' | 'completed' | 'expired'`) so the extract-as-string behavior cannot silently break. the fix already shipped in 0.7.7 (#17); this task adds the test that guards it.

## .source

full spec lives in the github issue (handoffs-at-source):
- ehmpathy/domain-objects-metadata#20
- read it: `gh issue view 20 --repo ehmpathy/domain-objects-metadata`

## .acceptance

- a test asset with a string-literal-union prop exists
- unit/integration test asserts it extracts as `string`, nullable/required preserved

## .see also

- src/domain.operations/extract/extractHomogeneousLiteralUnionType.ts
- src/domain.operations/extract/extractPrimaryTypeFromMemberDeclaration.ts
