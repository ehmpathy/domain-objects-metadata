# verification checklist: regression coverage for string-literal unions

## behavior coverage

no repros artifact declared for this behavior (pure test-coverage add). behaviors traced directly to wish acceptance + vision edgecases.

| behavior (from wish/vision) | test file | snapshots? | status |
|-----------------------------|-----------|------------|--------|
| required string-literal union → STRING | extractDomainObjectMetadataForDeclarationInFile.test.ts + extractPropertiesFromInterfaceDeclaration.test.ts | ✓ | ✅ |
| nullable string-literal union → STRING nullable | both files (verdict) | ✓ | ✅ |
| optional string-literal union → STRING required:false | both files (sortBy) | ✓ | ✅ |
| single string literal → STRING | interface test (mode) + orchestrator snapshot | ✓ | ✅ |
| number literal union → NUMBER | both files (priority) | ✓ | ✅ |
| boolean literal → BOOLEAN | both files (isActive) | ✓ | ✅ |
| array of literal union → ARRAY of STRING | both files (tags) | ✓ | ✅ |
| mixed literal union → throws BadRequestError | extractPropertiesFromInterfaceDeclaration.test.ts (MixedLiteralUnion) | ✓ (error snapshot) | ✅ |

## zero skips verified
- [x] no `.skip()` or `.only()` in the changed test files (grep-verified)
- [x] no silent credential bypasses (test is pure AST parse; no creds needed)
- [x] no prior failures carried forward — full extract suite green (42/42)

## snapshot coverage
- orchestrator grain: `extractDomainObjectMetadataForDeclarationInFile.test.ts.snap` — new ClaimSearch metadata block
- interface grain: `extractPropertiesFromInterfaceDeclaration.test.ts.snap` — updated string-literal-union block (added sortBy)
- both paired with explicit `toMatchObject` assertions (not snapshot-alone)

## snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| extractDomainObjectMetadataForDeclarationInFile.test.ts.snap | added | yes | new orchestrator-grain case for ClaimSearch; witnesses every literal-union coercion including sortBy required:false |
| extractPropertiesFromInterfaceDeclaration.test.ts.snap | modified | yes | added `sortBy` (STRING, required:false) to the extant string-literal-union case to prove required-preservation for the optional case |

- [x] every `.snap` change reviewed
- [x] intended changes have clear rationale
- [x] no accidental changes

## tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, "🎉 passed", no errors |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, "🎉 passed" |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, "🎉 passed" (biome + cycles + depcheck all clean after depcheck ignore fix) |
| unit (extract scope) | `rhx git.repo.test --what unit --scope path://extract --mode apply` | ✅ | exit 0, "42 passed, 0 failed, 0 skipped", 14 suites |

- [x] every test command was run (not assumed)
- [x] every output observed
- [x] exit codes verified (all green)
- [x] no tests skipped, mocked, or faked

note: no integration/acceptance suites apply — this is a pure AST-parse unit concern with no external boundary or contract endpoint. the repo has zero `.integration.test.ts` / `.acceptance.test.ts` for this domain.

## defect fixed during verification

**lint gap (prior, fixed per buttonup mandate)**: `depcheck` flagged three devDependencies (`rhachet-brains-fireworksai`, `rhachet-roles-bhrowser`, `rhachet-roles-ghlitch`) added to `package.json` by the practs bump already present in the working tree. these are declapract-managed role/brain packages consumed by `prepare:rhachet`, not via `src` imports — the same category as the 6 rhachet packages already ignored. added them to `.depcheckrc.yml`, mirroring the prior entries. lint now green.

## blockers
- none.
