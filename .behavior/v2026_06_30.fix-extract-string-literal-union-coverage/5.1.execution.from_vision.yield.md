# execution yield: regression coverage for string-literal unions

executes the vision at `1.vision.yield.md` — recommendation (b): add the absent orchestrator-grain coverage.

## todos

- [x] promote `ClaimSearch` test asset to a domain-object class so it can drive the class-based orchestrator path
- [x] add orchestrator-grain test case to `extractDomainObjectMetadataForDeclarationInFile.test.ts`
- [x] assert literal-union coercion: status/mode/name→STRING, verdict→STRING+nullable, priority→NUMBER, isActive→BOOLEAN, tags→ARRAY of STRING
- [x] generate + spot-check snapshot
- [x] verify unit suite green (interface-grain test unaffected by asset change)
- [x] verify types pass
- [x] verify lint (biome + cycles clean; only prior depcheck devDependency drift remains — out of scope)

## changes

### `src/domain.operations/.test.assets/ClaimSearch.ts`
- added `import { DomainLiteral } from 'domain-objects'`
- added `export class ClaimSearch extends DomainLiteral<ClaimSearch> implements ClaimSearch {}` (minimal Address-precedent form; the `DomainEntity` + `unique` first draft was pruned as YAGNI in self-review)
- added an optional literal-union prop `sortBy?: 'date' | 'relevance'` to the interface, to cover the `required: false` case (gap found in self-review)
- otherwise plain string/number/boolean literals only (per vision open-question 4: template-literal / enum unions are out of scope)

### `src/domain.operations/extract/extractDomainObjectMetadataForDeclarationInFile.test.ts`
- imported `DomainObjectPropertyType`
- added case `'should be able to extract domain object metadata for a literal with string literal unions'`
- explicit `toMatchObject` assertions on status, verdict, sortBy (required:false), priority, isActive, tags + `toMatchSnapshot()`

### `src/domain.operations/extract/extractPropertiesFromInterfaceDeclaration.test.ts`
- added a `sortBy` assertion (STRING, required:false) to the extant string-literal-union case, so the interface grain also proves required-preservation for the optional case

### `src/domain.operations/extract/__snapshots__/extractDomainObjectMetadataForDeclarationInFile.test.ts.snap`
- new snapshot for the ClaimSearch metadata — verified: every literal-union prop coerces to its correct primitive

## verification

| check | result |
|-------|--------|
| extract unit suite (`--scope path://extract`) | 42 passed, 0 failed |
| new orchestrator case | passes, assertions + snapshot |
| interface-grain unit test (asset reused) | unaffected, green |
| types (`--what types`) | pass |
| lint biome + cycles | pass |
| lint depcheck | prior devDependency drift (rhachet-brains-fireworksai, rhachet-roles-bhrowser, rhachet-roles-ghlitch) — introduced by the practs bump already in the working tree, not this work |

## scope notes

- followed vision recommendation (b) + asset strategy (ii): promote `ClaimSearch` rather than author a new asset — lowest churn, reuses the purpose-built literal-union fixture.
- test lives in the orchestrator-level `.test.ts` (repo has zero `.integration.test.ts`; disk-read orchestrator tests run under `--what unit`), per vision open-question 3.
- no production code changed — additive test coverage only, guards the #17 behavior.
