# blocker: reviewers thrash on non-actionable items; needs human `--as overruled`

## what blocks me

the `reviewed?` judge (`--allow-blockers 0 --allow-nitpicks 3`) halts. the substance is done
and verified, but the l3 deep reviewers re-classify pre-extant / merge-time / permission-gated
items as "blockers" and their count oscillates (r11: 4 → 6 blockers) **across passes where i was
actively fixing items** — they invent new angles each round rather than converge.

### state of the deliverable (all verified this session)

- **all 9 l1 reviewers approve 0/0** on the current code.
- **gates green**: types, format, lint, unit **81/81**.
- the one real, twice-confirmed structural blocker (peer-guard rigor asymmetry) is **fixed** and
  both l3 reviewers confirmed it resolved — shared `isObjectShapedAs` primitive across all three
  guards + mirrored absent-`of` / serialization tests.
- registry↔fixture drift gap **closed** with an exhaustiveness assertion (`CASES` vs `knownAliases`).
- serde-fns wisher-override **recorded in all three docs** (`0.wish.md`, `1.vision.yield.md`,
  `5.1.execution.from_vision.yield.md`) — r11 confirmed the wish + vision are now synced.

### the remaining r11 "6 blockers" — none are code defects in this behavior

| # | item | why not-actionable-by-me |
|---|------|--------------------------|
| 1 | split tooling churn (`package.json`/`.depcheckrc.yml`/`.claude/settings.json`) into a `chore` commit | **commit-boundary / merge-time** decision; the churn is pre-extant working-tree state, not this diff's logic |
| 2 | derive `CASES` from `knownAliases` structurally instead of the equality assertion | **gold-plate** — r11 itself admits my assertion "closes the drift-detection gap"; the explicit `CASES` array is the readable idiom |
| 3 | `ARRAY.of.of` hydration miss for a local-alias array (`dates: StandardDate[]`) | **pre-extant on `main`**, explicitly scoped out by the wish's "out of scope" hydrate-path gaps; registry aliases (this behavior's point) are unaffected |
| 4 | file 3 follow-up GitHub issues | needs `gh issue create`, **outside my pre-approved permissions** |
| 5 | untested local-alias-of-registry-alias throw path (`type MyId = Uuid`) | **pre-extant on `main`**, explicitly scoped out; same hydrate-path follow-up bucket |
| 6 | ~~`1.vision.yield.md` stale "serde-fns not added" line~~ | **FIXED this pass** — the one genuine miss; doc-sync now complete |

r10's nitpicks are the same merge-time / pre-extant set.

### why more re-arrivals will not help

- **budget is 8/9** — one unit left. the l3 reviewers thrash (4→6 blockers while items were fixed),
  so a re-arrival near-certainly lands >0 again on pre-extant/merge-time items, and exhausts budget.
- the blocking items are, by the reviewers' own words, "about traceability and structural coupling,
  not the happy path" — i.e. not extractor defects. no code change i can make within this behavior's
  scope drives them to 0.

## what i tried

- fixed the real peer-guard blocker per r11's exact proposed design (shared primitive + mirrored tests).
- added the registry↔fixture exhaustiveness assertion (the actionable half of r10/r11's coverage ask).
- synced the serde-fns override across all three behavior docs.
- installed serde-fns as a real pinned devDep + moved `DomainObjectPropertyType` to its own file
  (both human-directed this session), each with full gate re-verification.
- extended review budget 6→9 and drove three full fresh review cycles to converge what was convergeable.

## what i need

**`--as overruled`** (foreman-only) — the route provides this passage explicitly "to bypass reviewed?
judge thresholds for overzealous reviewers." this is that case: 0 l1 blockers, both l3 reviewers confirm
the core design sound and the one real blocker fixed, gates green, and every remaining "blocker" is
pre-extant-on-main, merge-time, or permission-gated. run:

`rhx route.stone.set --stone 5.1.execution.from_vision --route .behavior/v2026_06_30.fix-extract-serializable-type --as overruled`

then, at merge time (all recorded in the yield doc's dispositions): split the tooling churn into a
`chore` commit, file the follow-up issues (zod-v4 dead-validation; the two pre-extant hydrate-path
gaps), and add the uni-time `BREAKING CHANGE:` footer to the commit message. if you'd rather i file
the GitHub issues, grant `gh issue create` and i will.
