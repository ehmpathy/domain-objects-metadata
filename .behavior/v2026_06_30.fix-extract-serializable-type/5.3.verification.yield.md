# 5.3 verification — buttonup

## verification checklist

### behavior coverage

this is a library (no cli/api/app surface). the contract is the extracted metadata shape.
every promised behavior maps to a test with snapshot coverage.

**test-scope note (l1 reviewer fix):** the two behavior tests drive `ts.createProgram` over
`.test.assets/*.ts` fixtures — reads across the filesystem boundary. per
`rule.forbid.unit.remote-boundaries`, filesystem access disqualifies the `.test.ts` (unit)
suffix, so both are named `.integration.test.ts` and run under `jest.integration.config.ts`.

| behavior (from wish/vision) | test file | snapshots? | status |
|-----------------------------|-----------|------------|--------|
| each registry alias → `ALIAS { of: { name, primitive: STRING } }` (18 aliases, exhaustive divergent seaturtle fixtures) | `extract/extractPropertiesFromInterfaceDeclaration.aliases.integration.test.ts` | ✓ | ✅ |
| registry↔fixture exhaustiveness (no alias by proxy) | `extract/extractPropertiesFromInterfaceDeclaration.aliases.integration.test.ts` (`covers every knownAliases entry`) | n/a (assertion) | ✅ |
| nullable / optional / array flags preserved on ALIAS | `extract/extractPropertiesFromInterfaceDeclaration.aliases.integration.test.ts` | ✓ | ✅ |
| `Serializable` from real `serde-fns` dep | `.test.assets/SeaTurtleAliasZoo.ts` + aliases test | ✓ | ✅ |
| ALIAS is terminal through hydration | `hydrate/getHydratedDomainObjectMetadatasFromFiles.integration.test.ts` | ✓ | ✅ |
| local primitive alias unified to ALIAS (`PerformanceReport.onDate`) | `hydrate/getHydratedDomainObjectMetadatasFromFiles.integration.test.ts` (snapshot) | ✓ | ✅ |
| uni-time recognition removed; iso-time in its place | `.test.assets/NutrientResearchPublicationEvent.ts` + hydrate test/snap | ✓ | ✅ |
| object shapes (`IsoPriceShape`, `*Range`, `IsoDurationShape`) stay REFERENCE | `extract/extractPropertiesFromInterfaceDeclaration.aliases.integration.test.ts` | ✓ | ✅ |
| `ALIAS` property guard (`isDomainObjectAliasProperty`) | `guard/isDomainObjectAliasProperty.test.ts` | n/a | ✅ |
| shared shape primitive (`isObjectShapedAs`) | `guard/isObjectShapedAs.test.ts` | n/a | ✅ |
| `DomainObjectAliasMetadata` domain object | `domain.objects/DomainObjectAliasMetadata.test.ts` | n/a | ✅ |
| `DomainObjectPropertyMetadata` ALIAS variant | `domain.objects/DomainObjectPropertyMetadata.test.ts` | n/a | ✅ |

### zero skips verified
- [x] no `.skip()` or `.only()` found — `grep '\.(skip|only)\(' src` → no matches
- [x] no silent credential bypasses — library, no creds required
- [x] no prior failures carried forward — 73/73 pass, 0 skipped

### snapshot coverage for contract outputs

this library has no cli/app/sdk-method stdout; its contract is the metadata object shape.
snapshots capture the extraction + hydration output objects for pr vibecheck.

| contract surface | snapshot file | status |
|------------------|---------------|--------|
| alias extraction (all 18 + flag dims + REFERENCE boundary) | `extract/__snapshots__/extractPropertiesFromInterfaceDeclaration.aliases.integration.test.ts.snap` | ✅ |
| interface extraction (incl. iso-time fixture) | `extract/__snapshots__/extractPropertiesFromInterfaceDeclaration.test.ts.snap` | ✅ |
| hydration (ALIAS terminal + local-alias unify + iso-time) | `hydrate/__snapshots__/getHydratedDomainObjectMetadatasFromFiles.integration.test.ts.snap` | ✅ |

- [x] every extraction result snapshotted (`expect(properties).toMatchSnapshot()`)
- [x] every hydration result snapshotted
- [x] each output variant exercised (required, optional, nullable, array, REFERENCE boundary)
- [x] snapshots demonstrate actual output objects, paired with explicit assertions

### snapshot change rationalization

two `.snap` files carry content changes; both were then renamed alongside their test file to
`.integration.test.ts.snap` (git tracks the rename):

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| `extract/…aliases.integration.test.ts.snap` | added | yes | new exhaustive per-alias fixtures — the core deliverable |
| `hydrate/getHydratedDomainObjectMetadatasFromFiles.integration.test.ts.snap` | modified | yes | two hunks: (1) `PerformanceReport.onDate` unified `{STRING, of:'StandardDate'}` → `{ALIAS, of: DomainObjectAliasMetadata{name:'StandardDate', primitive:STRING}}` per wisher unify decision; (2) uni-time fixture swap — test title `UniDateTime`→`IsoTimeStamp`, `occurredAt` `{STRING, of:undefined}` → `{ALIAS, of:{name:'IsoTimeStamp', primitive:STRING}}` |

- [x] every `.snap` change reviewed
- [x] intended changes have clear rationale
- [x] no accidental changes

### tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✓ | 🎉 passed (9s) |
| format | `rhx git.repo.test --what format` | ✓ | 🎉 passed (4s) |
| lint | `rhx git.repo.test --what lint` | ✓ | 🎉 passed (26s), includes dpdm cycle check |
| unit | `rhx git.repo.test --what unit --mode apply` | ✓ | 🎉 passed (30s) — 11 suites, **40 passed, 0 failed, 0 skipped** |
| integration | `rhx git.repo.test --what integration --mode apply` | ✓ | 🎉 passed (116s) — 2 suites, **33 passed, 0 failed, 0 skipped** (the two fs-boundary behavior tests) |
| acceptance | n/a | — | no `.acceptance.test.ts` (pure library) |

combined: **73 passed, 0 failed, 0 skipped** across unit (40) + integration (33) — same total as before, split by scope after the l1 test-scope-purity fix.

- [x] every test command run (not assumed)
- [x] every output observed
- [x] exit codes verified (all pass)
- [x] no tests skipped, mocked, or faked

### contract output snapshot exhaustiveness

| contract | positive snapped? | negative/boundary snapped? | edge cases snapped? |
|----------|-------------------|----------------------------|---------------------|
| alias extraction | ✓ (all 18 aliases) | ✓ (object shapes → REFERENCE boundary) | ✓ (optional, nullable, array) |
| hydration | ✓ (ALIAS terminal) | ✓ (local-alias unify) | ✓ (iso-time swap) |

### blockers
- **resolved (peer review r008, mech-test-scope-purity, 2 blockers):** the two behavior tests used `ts.createProgram` to read `.test.assets/*.ts` from disk — a filesystem boundary — while named `.test.ts`, in breach of `rule.forbid.unit.remote-boundaries`. **fixed** by a rename of both (+ their snapshots) to `.integration.test.ts`, so they run under `jest.integration.config.ts`. re-verified: integration 33/33 pass, unit 40/40 pass. the rule exempts `.integration.test.ts`, so the blocker is cleared at the source, not argued.
- the 5.1 execution stone's residual l3 items were foreman-`overruled` (pre-extant-on-main hydrate gaps, merge-time commit hygiene, permission-gated issue creation) — all recorded in `5.1.execution.from_vision.yield.md` dispositions and `blocker/5.1.execution.from_vision.md`. none is a verification gap in this behavior's scope.
