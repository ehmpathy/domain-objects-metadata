# 1.upgrade.invoke — iteration 1

## status

upgrade completed successfully

## what happened

1. ran `rhx declapract.upgrade exec`
2. initial run hit corrupted `node_modules/asynckit/package.json` (json parse error)
3. removed node_modules via `rhx rmsafe --path node_modules --recursive`
4. reinstalled via `pnpm install`
5. re-ran upgrade — completed successfully

## packages upgraded

- declapract: 0.11.5 → 0.13.21
- declapract-typescript-ehmpathy: 0.33.15 → 0.47.74

## changes applied

- directory structure: `src/domain/` → `src/domain.objects/`, `src/logic/` → `src/domain.operations/`
- test assets: `__test_assets__/` → `.test.assets/`
- acceptance: `acceptance/` → `blackbox/`
- many cicd workflow updates
- format tools: prettier → biome
- new files: biome.jsonc, license.md, provision/github.repo/resources.ts

## warnings

- 73 biome lint warnings (not blockers, will be addressed in hazards detection)
- peer dependency warnings (typescript version mismatch with some packages)
- deprecation warnings on some dependencies (uuid, eslint, etc.)

## notes

the old node_modules had a corrupted package.json which caused the initial failure. clean and reinstall resolved it.
